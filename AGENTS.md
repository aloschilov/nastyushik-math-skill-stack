# Repo Instructions For Codex

## Формат математических формул

Все новые математические выражения в заданиях, ответах, обратной связи, Markdown-документах и строках генераторных скриптов нужно задавать в LaTeX-нотации.

- Inline-формулы оформлять как `\( ... \)`.
- Отдельные многострочные формулы оформлять как `\[ ... \]`.
- Не писать формулы в plain text вроде `x^2`, `<=`, `>=`, `->`, `*`, если это не объяснение синтаксиса.
- Использовать `\(x^2\)`, `\(\le\)`, `\(\ge\)`, `\(\Rightarrow\)`, `\(\cdot\)`, `\(50^\circ\)`.
- Десятичные дроби в русской записи писать через запятую внутри LaTeX: `\(1{,}5x\)`, `\(0{,}5x \cdot 8x^2 = 4x^3\)`.
- В пользовательских PDF формулы должны быть отрендерены как математические символы или изображения, а не показаны буквальным текстом `\(...\)`.
- В пользовательских PDF не оборачивать формулы в Markdown backticks. Backticks допустимы только когда обсуждается сам синтаксис LaTeX или имя команды.

Текущий стандарт репозитория: LaTeX-исходник в Markdown и отрендеренные формулы в PDF.

## Предметные capability matrix

- Алгебра, геометрия, теория вероятностей и статистика ведутся в одном репозитории как единый математический контур.
- Основной curriculum target с (7)-го класса — федеральная программа углублённого уровня 2025 года. Локальный источник: **asserts/FRP_matematika_7-9_uglublennyi_uroven_2025.pdf**.
- Последовательность и рекомендованные часы разделов хранятся в **data/curriculum/advanced_grade7.csv**. Базовые контрольные с суффиксом `7_klass` используются как нижний диагностический порог, а не как верхняя цель.
- Источники истины разделены по направлениям:
  - **data/capability_matrices/algebra.csv**;
  - **data/capability_matrices/geometry.csv**;
  - **data/capability_matrices/probability_statistics.csv**.
- Общий дневной комплект может включать задания из нескольких направлений, но каждое наблюдение и изменение статуса нужно записывать только в соответствующую предметную матрицу.
- Не переносить уровень автоматически между предметами. Успех в алгебраическом доказательстве не повышает геометрическое доказательство или вероятностное рассуждение без отдельного evidence.
- Статус **NEW** и уровень \(0\) означают отсутствие диагностики, а не отсутствие способности.
- Не включать все три направления в каждый день механически. Выбирать один основной предметный gate и при необходимости один короткий поддерживающий перенос.
- При автоматическом выборе основной дисциплины ориентироваться на пропорцию программы (4:3:1): четыре алгебраических, три геометрических и один вероятностно-статистический основной день в скользящем окне из восьми учебных дней.
- Незавершённая содержательная мысль, ответ «не знаю» или новая принципиальная ошибка из последней работы имеют приоритет над пропорцией на один следующий день. Сначала дать конкретный источник или короткое введение, затем близкую задачу и перенос.
- Внутри выбранного направления брать первый незакрытый раздел по `sequence` из **data/curriculum/advanced_grade7.csv**, если последняя работа не требует вернуться к более ранней предпосылке.
- Углублённый уровень и олимпиадный слой не смешивать: программа задаёт обязательный маршрут, а олимпиадная задача добавляется как перенос идеи, но не заменяет освоение curriculum gate.

## Генерация дневных материалов

- Для новых дневных комплектов сохранять существующую структуру: фидбек ребёнку, фидбек родителю, задания следующего дня, ответы и акценты.
- Preferred workflow для новых дней: Markdown-исходники в `artifacts/generated/source/dayNN/*.md` и сборка через `python3 scripts/build_day_pdfs.py --day NN --update-manifest --render-preview`.
- Каждый Markdown-исходник должен иметь front matter с `title:` и `output:`. `output:` указывает итоговый PDF внутри `artifacts/generated/...`.
- В `tasks`-исходнике нового дня дополнительно указывать `primary_domain:` и `curriculum_unit:`. Допустимые значения `primary_domain`: `algebra`, `geometry`, `probability_statistics`.
- Использовать общий шаблон `templates/day-material.tex`; не плодить отдельный PDF renderer без необходимости.
- Старые `scripts/generated/make_day*_pdfs.py` можно оставлять как исторические генераторы. Для нового дня не копировать ReportLab-подход, если нет специальной причины.
- Если всё же формула попадает в `reportlab` `Paragraph`, рендерить её перед вставкой в PDF. Допустимый fallback: превращать фрагменты `\( ... \)` в inline PNG через установленный TeX (`latex` + `dvipng`) и вставлять через `<img>`.
- Нельзя сдавать PDF, где ребёнок видит сырые LaTeX-маркеры `\(`, `\)`, `\le`, `\ge`, `\Rightarrow`, `\circ`, `\cdot`.
- Старые PDF и старые сгенерированные скрипты не переписывать только ради смены нотации.

## Проверки

После изменения repo-level инструкций или prompt-шаблонов запускать:

```bash
python3 scripts/validate_matrix.py
git diff --check
```

Если задача связана с генерацией PDF, дополнительно:

```bash
python3 scripts/build_day_pdfs.py --day NN --update-manifest --render-preview
pdftotext path/to/file.pdf - | rg '\\\\\(|\\\\\)|\\\\le|\\\\ge|\\\\Rightarrow|\\\\circ|\\\\cdot|[A-Za-z]\^[0-9]'
```

Команда не должна находить совпадений в пользовательских материалах. После этого рендерить новые PDF через `pdftoppm` и визуально проверять страницы.
